# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This is a scaffold repository for a 3-day team hackathon (team B). As of now it contains no application code, no build tooling, and no test suite — just the README and a few placeholder files from initial setup. There are no build/lint/test commands to run yet. When actual project code is added, this file should be updated with the real commands and architecture notes.

## Workflow

Per the README, the intended workflow is: branch off, do the work, open a PR to merge (`ブランチを切って作業し、PR でマージする`).

- `upstream` remote points to the team repo (`jigintern/202609-3days-hackathon-team-b`); `origin` points to a personal fork. New branches should be based on `upstream/main`.
- PR descriptions should be written in Japanese.
